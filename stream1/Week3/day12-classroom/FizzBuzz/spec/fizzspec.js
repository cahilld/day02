describe("fizzBuzz", function() {
        it("Should return 'FizzBuzz'", function() {
            expect(fizzBuzz(-1)).toBe('Sorry. I can’t tell what drink because that age is incorrect!')
        });
});